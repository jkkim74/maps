# 앱이 호출할 API 오리진.
# - 개발(npm run dev): 비워두면 Vite 프록시가 /api를 127.0.0.1:8000으로 전달.
# - 프로덕션 빌드(cap sync/네이티브): 비워두면 src/config.ts 기본값(https://magable.kr) 사용.
#   다른 서버로 빌드하려면 여기에 절대 URL을 지정(예: VITE_API_BASE_URL=https://staging.example.kr).
VITE_API_BASE_URL=
