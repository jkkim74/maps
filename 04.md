# Phase 4: Mock 거래 + RiskManager Kill Switch

Phase 3이 완료된 상태에서 진행.
pytest 전체 실행 후 통과 확인하고 시작해줘.

## 작업 목표
MockBroker, OrderManager, RiskManager(Kill Switch)를 만든다.
실계좌 주문 전에 이 단계가 완전히 검증되어야 한다.
Phase 5(실브로커)는 이 단계 이후에만 진행.

## Step 1: execution/broker_adapter.py
BrokerAdapter ABC:
- place_order(order) -> OrderResult
- cancel_order(order_id) -> bool
- get_position(ticker) -> Position
- get_account_balance() -> AccountBalance
- is_market_open() -> bool
- subscribe_realtime: NotImplementedError (Phase 5용)

## Step 2: execution/mock_broker.py
MockBroker(BrokerAdapter):
- initial_cash = 100_000_000
- place_order: Kill Switch 체크 -> 중복 주문 체크 -> 즉시 체결
- cancel_order, get_position, get_account_balance 구현
- eod_cleanup(): 미체결 주문 전량 취소
- activate_kill_switch() / deactivate_kill_switch(approved_by)
- is_market_open(): 평일 09:00~15:30 KST

## Step 3: risk/manager.py
RiskManager:
- check_before_order(order, account): 일 손실 한도(1.5%), 노출 한도 체크
- on_order_failure(): 연속 실패 카운트 + 5회 시 Kill Switch 자동 발동
- on_order_success(): 카운트 리셋
- deactivate(approved_by): 관리자 승인 후 해제
- _trigger_kill(reason, value): kill_switch_log 기록 + 알림

## Step 4: execution/order_manager.py
OrderManager:
- broker: BrokerAdapter (MockBroker 주입)
- risk: RiskManager
- submit(order): risk.check_before_order -> broker.place_order
  실패 시 risk.on_order_failure, 성공 시 risk.on_order_success
- cancel(order_id), eod_cleanup()
- Research 전략 주문 시도 -> ResearchStrategyError

## Step 5: 테스트 작성 (테스트 먼저!)
tests/test_mock_broker.py:
- test_place_order_filled
- test_duplicate_order_error
- test_kill_switch_blocks_order
- test_eod_cleanup_cancels_all
- test_insufficient_cash_rejected

tests/test_risk_manager.py:
- test_daily_loss_trigger: 1.5% 초과 시 Kill Switch
- test_consec_failure_trigger: 5회 연속 실패 시 Kill Switch
- test_deactivate_requires_approval
- test_exposure_cap: 단일 종목 10% 초과 차단

tests/test_order_manager.py:
- test_research_strategy_blocked
- test_kill_switch_propagates

## Mock 3개월 시뮬레이션
위 테스트 통과 후:
"pullback_v3 전략으로 MockBroker 환경에서
2025-01-01~2025-03-31 기간 시뮬레이션을 실행하고
일별 자산곡선, 총 거래 수, MDD, Sharpe를 출력해줘."

## 완료 기준
- pytest 전부 통과
- Mock 3개월 시뮬레이션 완료
- PromotionGate Mock->Live Small 통과 여부 확인