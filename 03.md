# Phase 3: 검증 엔진

Phase 2가 완료된 상태에서 진행.
pytest 전체 실행 후 통과 확인하고 시작해줘.

## 작업 목표
WalkForwardAnalyzer, ParameterPlateauTester, MonteCarloSequenceTester,
BlockBootstrapTester, PromotionGate를 구현한다.
특히 WFA 4조건과 PromotionGate 누락 체크는 설계서 핵심 사항.

## Step 1: validation/walk_forward.py
WalkForwardAnalyzer:
- run(data, strategy, param_grid) -> WFAResult
- 5-fold, IS 3년, OOS 1년
- fold별: is_sharpe, oos_sharpe, is_g2p, oos_g2p 모두 기록
- 4조건 AND 판정:
  checks = {
    'sharpe_mean_positive': sharpe_mean > 0,  # 필수!
    'sharpe_cv_ok': cv <= 0.5,
    'neg_fold_count_ok': neg_count <= 1,
    'oos_is_g2p_ok': g2p_ratio >= 0.6,
  }
  pass = all(checks.values())
- walk_forward_results 테이블에 저장

## Step 2: validation/plateau.py
ParameterPlateauTester:
- run(results_grid) -> PlateauResult
- 이웃 = Manhattan distance 1 이내
- 통과 조건: metric >= best×0.75 AND mdd <= best_mdd×1.25
- grade_plateau(score): robust(>=70)/moderate(40~70)/fragile(<40)

## Step 3: validation/monte_carlo.py + block_bootstrap.py
MonteCarloSequenceTester: n=1000, 거래 순서 랜덤 셔플
BlockBootstrapTester: block_size=[5,10,20]
둘 다 CostModel 적용 후 MDD 분포 반환 (p50, p75, p90, p95)
BBoot/MC 비율 >= 1.5 이면 "군집성 위험" 경고

## Step 4: promotion/gate.py (v2.6.2 강화본 기준)
evaluate_promotion(strategy, current_stage, history):
- PROMOTION_GATES에서 gate 로드
- 모든 체크를 실제로 수행 (mc_within_limit 포함!)
- 분모 0 방어: is_cagr<=0, mock_sharpe<=0, assumed_slip==0 즉시 fail
- 모르는 strategy_id -> UnknownStrategyError
- 없는 메트릭 -> fail with reason (KeyError로 죽으면 안 됨)
- 결과를 promotion_history에 기록

## Step 5: 테스트 작성 (테스트 먼저!)
tests/test_walk_forward.py:
- test_wfa_negative_mean_blocked: 모든 fold -0.3 -> fail
- test_wfa_4_conditions_and: 4조건 중 1개 깨지면 fail
- test_wfa_zero_division: mean==0이면 cv=inf -> fail

tests/test_promotion_gate.py:
- test_mc_within_limit: MC MDD 초과 시 mock->live_small fail
- test_zero_division_cagr: is_cagr=0 -> fail
- test_unknown_strategy_id: UnknownStrategyError 발생
- test_all_checks_executed: gate에 정의된 모든 체크가 실행됨

## 완료 기준
- pytest 전부 통과
- WFA + Plateau + MC + BBoot 순차 실행 성공
- PromotionGate가 pullback_v3 전략에 대해 올바른 판정 반환