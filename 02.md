# Phase 2: 백테스트 엔진 + 비용 모델

Phase 1이 완료된 상태에서 진행.
pytest 전체 실행 후 통과 확인하고 시작해줘.

## 작업 목표
CostModel과 BacktestEngine을 만들고,
파일럿 전략 pullback_v3로 10년 백테스트가 동작하게 한다.

## Step 1: backtest/cost_model.py
CostModel 클래스:
- TRANSACTION_TAX_SELL = 0.0018 (코스피·코스닥, ETF 면제)
- BROKER_FEE_ROUNDTRIP = 0.00015
- SLIPPAGE_LARGE_CAP = 0.0005 (시총 >= 5천억)
- SLIPPAGE_SMALL_CAP = 0.0015
- apply(trade) -> net_pnl
- sensitivity(trade, multiplier) -> net_pnl (±50% 시나리오)

## Step 2: strategy/base.py
BaseStrategy 추상 클래스:
- strategy_id: str (클래스 변수)
- strategy_group: str (STRATEGY_GROUP_MAP 참조)
- generate_signals(data, params) -> DataFrame
  (컬럼: entry_signal, exit_signal, stop_price)
- param_grid() -> List[dict]
- default_params property

## Step 3: strategy/pullback_v3.py
PullbackV3Strategy 구현:
진입 3조건 AND:
  (1) MA5 > MA20
  (2) RSI(2) < rsi_threshold (기본 10)
  (3) low < 전일 low
청산: close >= MA5 (보유 1일 이상)
손절: 진입가 대비 -5%
param_grid: rsi_threshold=[5,10,15], ma_long=[20,30,40]

## Step 4: backtest/engine.py
BacktestEngine 클래스:
- run(strategy, params, data, universe, cost_model) -> BacktestResult
- BacktestResult: cagr, mdd, sharpe, gain_to_pain, trade_list
- PositionSizingEngine 내장:
  계좌 위험 0.5% / (진입가 - 스탑가)
  단일 종목 노출 10% 상한

## Step 5: 테스트 작성 (테스트 먼저!)
tests/test_cost_model.py:
- test_etf_no_tax: ETF는 거래세 0
- test_small_cap_slippage: 시총 < 5천억이면 SLIPPAGE_SMALL_CAP 적용
- test_sensitivity: sensitivity(trade, 1.5) > apply(trade)

tests/test_backtest.py:
- test_pullback_signals: 3조건 AND로 신호 생성 확인
- test_no_future_leak: 신호가 당일 종가 이후 데이터를 참조하지 않음
- test_param_grid: param_grid() 반환 조합 수 = 3×3 = 9

## 완료 기준
- pytest 전부 통과
- pullback_v3 10년 백테스트 30분 이내 완료
- BacktestResult에 trade_list 포함 (MC 테스트용)